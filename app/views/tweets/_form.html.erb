<% if current_user %>
    <%= form_for Tweet.new do |f| %>
      <%= f.label :message  %>
      <%= f.text_field :message, placeholder:"what needs doing?", :class => 'tweet_area' %>
      <%= f.submit %>
    <% end %>
<% end %>
